@model IEnumerable<mert_polat_211103040.DAL.avukatlar>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<h2></h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.içerik)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.içerik)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isim)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.avukatid }) |
            @Html.ActionLink("Details", "Details", new { id=item.avukatid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.avukatid })
        </td>
    </tr>
}

</table>
