@model IEnumerable<mert_polat_211103040.DAL.içerik>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<h2></h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.içerik1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.içerik1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.kariyerid }) |
            @Html.ActionLink("Details", "Details", new { id=item.kariyerid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.kariyerid })
        </td>
    </tr>
}

</table>
